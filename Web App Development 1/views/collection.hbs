{{> menu}}
{{#with singleCollection}}

<section class="ui center aligned middle aligned segment">
  <h2 class="ui header">
    {{title}}
  </h2>
  
  <div class="ui card fluid">
    <div class="content">
      {{> listconsoles}}
      {{> addconsole}}
    </div>
  </div>
</section>

{{/with}}